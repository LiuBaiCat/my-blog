---
title: 2.JavaScript入门到精通
date: 2024-07-28
tags: [JavaScript, 基础学习]
description: JavaScript的基础学习
pinned: false
---
# JavaScript入门到精通

## 01.对象的方法

<img src="/img_02/Snipaste_2024-09-14_14-08-24.png" alt="Snipaste_2024-09-14_14-08-24" style="zoom:38%;" />

```javascript
    let obj = {
        uname: '蔡徐坤',
        saying: '你干嘛，哎哟',
        hobby:function () {
            console.log('唱');
            console.log('跳');
            console.log('Rap');
            console.log('篮球');
        }
    }
    // obj.hobby();
    //obj.uname ==== obj['uname']
    for(let E in obj){
        console.log(E);
    }
    for (let E in obj){
        console.log(obj[E]);
    }
```

## 02.遍历数组对象

<img src="/img_02/Snipaste_2024-09-14_21-01-55.png" alt="Snipaste_2024-09-14_21-01-55" style="zoom:38%;" />

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table {
            width: 600px;
            text-align: center;
        }
        table,th,td{
            border: 1px solid #ccc;
            border-collapse: collapse;
        }
        caption{
            font-style: 18px;
            margin-bottom: 10px;
            font-weight: 700;
        }
        tr{
            height: 40px;
            cursor: pointer;
        }
        table tr:nth-child(1){
            background-color: #ddd;
        }

        table tr:not(:first-child):hover{
            background-color: #eee;
        }
    </style>
</head>
<body>
    <h2>学生信息</h2>
    <table>
        <caption>学生列表</caption>
        <tr>
            <th>序号</th>
            <th>姓名</th>
            <th>年龄</th>
            <th>性别</th>
            <th>家乡</th>
        </tr>
        <script>
           let stu = [
            {name:'小明',age:18,gender:'男',hometown:'河北省'},
            {name:'小红',age:19,gender:'女',hometown:'河南省'},
            {name:'小刚',age:17,gender:'男',hometown:'山西省'},
            {name:'小丽',age:18,gender:'女',hometown:'山东省'},
        ] 
        for (let i = 0; i < stu.length; i++) {
            document.write(`
            <tr>
                <td>${i + 1}</td>
                <td>${stu[i].name}</td>
                <td>${stu[i].age}</td>
                <td>${stu[i].gender}</td>
                <td>${stu[i].hometown}</td>
            </tr>
            `)
            
        }
        </script>
    </table>
</body>
</html>
```

## 03.数学内置对象

```javascript
		console.log(Math.PI);
		//3.141592653589793
        console.log('---------------------');
        //ceil 天花板 向上取整
        console.log(Math.ceil(1.1));
		//	2
        console.log('---------------------');
        //floor 地板
        console.log(Math.floor(1.9));
		//	1
        console.log('---------------------');
        //四舍五入 round
        console.log(Math.round(1.4));
		//	1
        console.log(Math.round(1.5));
		//	2
        console.log('---------------------');
        console.log(Math.max(1,2,3,4,5));
		//	5
        console.log('---------------------');
		//	1
        console.log(Math.min(1,2,3,4,5));
```

## 04.随机数函数

```javascript
//0~10 之间随机整数
        console.log(Math.floor(Math.random() * 11));
        //5~10 之间的随机数
        console.log(Math.floor(Math.random() * (10 - 5 + 1)) + 5);
        //M~N
        let M = 5,N = 10;
        console.log(Math.floor(Math.random() * (N - M + 1)) + M);

        let arr = ['red','green','blue'];
        let random = Math.floor(Math.random() * arr.length);
        console.log(arr[random]);
```

## 05.随机点名

```javascript
let arr = ['赵云', '黄忠', '关羽', '张飞', '马超', '刘备', '曹操'];
        let random = Math.floor(Math.random() * arr.length);
        document.write(arr[random]);

        arr.splice(random, 1);
        console.log(arr);
        // splice() 方法用于添加或删除数组中的元素。
        // 注意：这种方法会改变原始数组。
        //---------------------------------------------------
        // 实例
        // 数组中添加新元素：
        // var fruits = ["Banana", "Orange", "Apple", "Mango"];
        // fruits.splice(2, 0, "Lemon", "Kiwi");
        // fruits 输出结果：
        // Banana, Orange, Lemon, Kiwi, Apple, Mango
```

## 06.自定义属性

<img src="/img_02/Snipaste_2024-09-14_21-26-01.png" alt="Snipaste_2024-09-14_21-26-01" style="zoom:38%;" />

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div data-id="1" data-spm="不知道">1</div>
    <div data-id="2">2</div>
    <div data-id="3">3</div>
    <div data-id="4">4</div>
    <div data-id="5">5</div>

    <script>
        const two = document.querySelector('div');
        console.log(two.dataset);

        const one = document.querySelectorAll('div');
        // console.log(one.dataset.id);
        // console.log(one.dataset.spm);
        console.log(one);
        console.log(one[0].dataset.id);
    </script>
</body>
</html>
```

## 07.定时器-间歇函数

```javascript
function fn(){
            document.write('hello');
        }
        // setInterval(fn,1000);
        // setInterval('fn()',1000);

        let n = setInterval(fn,1000);

        // clearInterval(n);
```

## 08.事件监听

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <button>点击</button>
    <script>
        const btn = document.querySelector('button');
        btn.addEventListener('click',function () {
            alert('hello');
        })
    </script>
</body>
</html>
```

## 09.键盘事件类型

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <input type="text">
    <script>
        const input = document.querySelector('input');
        input.addEventListener('keydown',function () {
            console.log("键盘按下了");
            
            // 检查是否按下了“a”键  
            if (event.key === 'a') {  
                console.log("字母 'a' 被按下了");  
            }  
        })

        input.addEventListener('keyup',function () {
            console.log("键盘弹起了");
        })
    </script>
</body>
</html>
```

## 10.文字统计

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .input{
            width: 100px;
            transition: all .3s;
        }

        .input:focus{
            width: 200px;
            /* min-height: 32px;
            max-height: 100px; */
        }
        .total{
            opacity: 0;
            color: #6e6e6e;
            position: absolute;
            left: 100px;
        }
        .over-limit {  
            color: red;  
        } 
    </style>
</head>
<body>
    <!-- <input type="text" id="tx"> -->
    <textarea class="input" id="tx"></textarea>
    <div class="total">0/200字</div>
    <script>
        const tx = document.querySelector('#tx');
        const total = document.querySelector('.total');
        tx.addEventListener('focus',function(){
            total.style.opacity = 1;
        })

        tx.addEventListener('blur',function(){
            total.style.opacity = 0;
        })

        tx.addEventListener('input',function(){


            if (tx.value.length >= 200) {  
                total.classList.add('over-limit');  
                tx.value = tx.value.substring(0, 200);  
            } else {  
                total.classList.remove('over-limit');  
            }

            total.innerHTML = `${tx.value.length}/200字`;
        })
    </script>
</body>
</html>
```

## 11.环境对象(this)

```html
    <button>点击</button>
    <script>
        const btn = document.querySelector('button');
        btn.addEventListener('click',function(){
            // console.log(this);
            // btn.style.color = 'red';
            this.style.color = 'red';
        })
    </script>
```

## 12.全选案例

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
        }
        table{
            border-collapse: collapse;
            border-spacing: 0;
            border: 1px solid #c0c0c0;
            width: 500px;
            margin: 100px auto;
            text-align: center;
        }
        th{
            background-color: #09c;
            font: bold 16px "微软雅黑";
            color: #fff;
            height: 24px;
        }
        td{
            border: 1px solid #d0d0d0;
            color: #404060;
            padding: 10px;
        }
        .allCheck{
            width: 80px;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th class="allCheck">
                <input type="checkbox" name="" id="checkAll"><span class="all">全选</span>
            </th>
            <th>商品</th>
            <th>商家</th>
            <th>价格</th>
        </tr>
        <tr>
            <td>
                <input type="checkbox" name="check" id="ck">
            </td>
            <td>小米手机</td>
            <td>小米</td>
            <td>￥1999</td>
        </tr>
        <tr>
            <td>
                <input type="checkbox" name="check" id="ck">
            </td>
            <td>小米净水器</td>
            <td>小米</td>
            <td>￥4999</td>
        </tr>
        <tr>
            <td>
                <input type="checkbox" name="check" id="ck">
            </td>
            <td>小米电视</td>
            <td>小米</td>
            <td>￥5999</td>
        </tr>
    </table>
    <script>
        const checkall = document.querySelector('#checkAll');
        const cks = document.querySelectorAll('#ck');
        checkall.addEventListener('click',function(){
            //得到大复选框的选中状态
            // console.log(this.checked);
            for (let i = 0; i < cks.length; i++) {
                cks[i].checked = this.checked;
            }
        })
        //小勾选框控制大复选框
        for (let i = 0; i < cks.length; i++) {
            cks[i].addEventListener('click',function(){
                //判断选中的小复选框个数 ？= all
                // console.log(document.querySelectorAll('#ck:checked').length);
                checkall.checked = document.querySelectorAll('#ck:checked').length === cks.length;
            })
            
        }
    </script>
</body>
</html>
```

## 13.事件流以及阻止冒泡

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .father{
            width: 500px;
            height: 500px;
            background-color: pink;
        }
        .son{
            width: 200px;
            height: 200px;
            background-color: purple;
        }
    </style>
</head>
<body>
    <div class="father">
        <div class="son"></div>
    </div>
    <script>
        const father = document.querySelector('.father');
        const son = document.querySelector('.son');
        document.addEventListener('click',function(){
            alert('我是爷爷');
        })
        father.addEventListener('click',function(e){
            alert('我是爸爸');
            e.stopPropagation();
        })
        son.addEventListener('click',function(e){
            alert('我是儿子');
            e.stopPropagation();
        })
    </script>
</body>
</html>
```

## 14.事件解绑

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <button>点击</button>
    <script>
        const btn = document.querySelector('button');
        // btn.onclick = function(){
        //     alert('点击了');
        //     btn.onclick = null;
        // }
        function fn() {
            alert('点击了');
            btn.removeEventListener('click',fn);
        }
        btn.addEventListener('click',fn);
    </script>
</body>
</html>
```

## 15.事件注册

<img src="/img_02/Snipaste_2024-09-14_21-53-47.png" alt="Snipaste_2024-09-14_21-53-47" style="zoom:38%;" />

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        ul{
            background-color: #3cd1ff80;
            width: 200px;
        }
        li:nth-child(odd) {  
            /* odd：单号颜色，也可以是2n+1 */
            background-color: rgb(200, 255, 0);  
        } 
        li:nth-child(even) {  
            background-color: yellowgreen;  
        }
        li,p{
            cursor: pointer;
            list-style-type: none;
            width: 200px;
        }
        p:hover,li:hover{
            background-color: #c6c6c6;
        }
    </style>
</head>
<body>
    <ul>
        <li class="yuanshen">第1个孩子</li>
        <li>第2个孩子</li>
        <li>第3个孩子</li>
        <li>第4个孩子</li>
        <li>第5个孩子</li>
        <p>我不需要变色</p>
    </ul>
    <script>
        const ul = document.querySelector('ul');
        ul.addEventListener('click',function(e){
            // alert(11);
            // console.log(e.target);
            // console.dir(e.target);
            // if(e.target.className === 'yuanshen')alert('wc O!');
            
            if(e.target.tagName === 'LI')
            e.target.style.color = 
                (e.target.style.color =='red') ? 'black' : 'red';
        })
    </script>
</body>
</html>
```

## 16.阻止默认行为

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="http://www.bilibili.com">
        <p>密码为（114514）</p>
        <input type="password" class="intext" value="">
        <input type="submit" value="登录" id="tijiao">
    </form>
    <script>
        const tijiao = document.querySelector('#tijiao');
        tijiao.addEventListener('click',function(e){
            // e.preventDefault();
            const intext = document.querySelector('.intext');
            const pass = intext.value;
            console.log(pass);
            if(pass != '114514'){
            e.preventDefault();
            // location.reload();
            intext.value = '';
            alert('密码错误');
        }
            
        })
        
    </script>
</body>
</html>
```

## 17.页面加载事件

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        // //等待页面所有资源加载完毕，就回去执行回调函数
        // window.addEventListener('load',function(){
    //######(window)

        //     const btn = document.querySelector('button');
        //     btn.addEventListener('click',function(){
        //         alert('hello');
        //     })
        // })

        //######################################################

        // img.addEventListener('load',function(){
        //     //等待图片加载完毕，再去执行里面的代码
        // })

        //######################################################

        //不需要等待页面所有资源加载完毕，就回去执行回调函数
        document.addEventListener('DOMContentLoaded',function(){
    //######(document)
            const btn = document.querySelector('button');
            btn.addEventListener('click',function(){
                alert('hello');
            })

        })
    </script>
</head>
<body>
    <button>点击</button>
</body>
</html>
```

## 18.页面滚动事件

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            padding-top: 100px;
            height: 3000px;
        }
        div{
        display: none;
            margin: 100px;
            overflow: scroll;
            width: 200px;
            height: 200px;
            border: 1px solid #000;
        }
    </style>
</head>
<body>
    <div>
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
        这里面有很多很多的文字
    </div>
    <script>
        const div = document.querySelector('div');
        // //页面滚动事件
        window.addEventListener('scroll',function(){
            // console.log('我滚了');
            const n = document.documentElement.scrollTop;
            // <100px就隐藏
            if(n >= 100){
                div.style.display = 'block';
            }else{
                div.style.display = 'none';
            }
        })

        // const div = document.querySelector('div');
        // div.addEventListener('scroll',function(){
        //     // console.log(111);
        //     console.log(div.scrollTop);
        //     // scrollLeft,scrollTop获取元素内容往左往上滚出去看不到的距离
        // })
    </script>
</body>
</html>
```

## 19.页面尺寸事件(获取尺寸)

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        div{
            display: inline-block;
            /* width: 200px; */
            height: 200px;
            background-color: pink;
            padding: 10px;
            border: 20px solid red;
        }
    </style>
</head>
<body>
    <div>12345678091235689012356890123456890</div>
    <script>
        const div = document.querySelector('div');
        console.log(div.clientWidth);
        // clientHeight,clientWidth获得元素的可见部分宽高

        // resize 浏览器窗口大小发生变化的时候触发的事件
        window.addEventListener('resize',function(){
            console.log(1);
        })
    </script>
</body>
</html>
```

## 20.offsetLeft、offsetTop(检测盒子离 <u>最近一级带有定位的祖先元素</u> 的位置)

<img src="/img_02/Snipaste_2024-09-14_22-02-58.png" alt="Snipaste_2024-09-14_22-02-58" style="zoom:38%;" />

```html
<style>
        div{
            position: relative;
            width: 200px;
            height: 200px;
            background-color: pink;
            margin: 100px;
        }
        p{
            width: 100px;
            height: 100px;
            background-color: purple;
            margin: 50px;
        }
    </style>
</head>
<body>
    <div>
        <p></p>
    </div>
    <script>
        const div = document.querySelector('div');
        const p = document.querySelector('p');
        // console.log(div.offsetLeft);
        // console.log(div.offsetTop);

        //检测盒子的位置 最近一级带有定位的祖先元素
        console.log(p.offsetLeft);
    </script>
</body>
</html>
```

## 21.实例化日期对象

```javascript
		// 得到当前时间
         const date = new Date();
         console.log(date);

        // 得到指定时间
         const date1 = new Date('2022-5-1 08:30:00');
         console.log(date1);

        const date = new Date();
        console.log(date.getFullYear());
        //月份取值为 0 ~ 11 !!!!!!!!
        console.log(date.getMonth() + 1);
        console.log(date.getDate());
        //星期天是 0 !!!!!!!!!!!(0 ~ 6)
        console.log(date.getDay());
```

## 22.显示格式化的时间

### ①方法一

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        div{
            display: inline-block;
            /* width: 300px; */
            height: 40px;
            border: 1px solid pink;
            text-align: center;
            line-height: 40px;
        }
    </style>
</head>
<body>
    <div></div>
    <script>
        const div = document.querySelector('div');
        function getMyDate(){
            const date = new Date();
            
            const year = date.getFullYear();
            const mon = date.getMonth() + 1;
            const day = date.getDate();

            let hour = date.getHours();
            let min = date.getMinutes();
            let sec = date.getSeconds();

            hour = hour < 10 ? '0' + hour : hour;
            min = min < 10 ? '0' + min : min;
            sec = sec < 10 ? '0' + sec : sec;

            const weeklist = ['天','一','二','三','四','五','六']
            const week = weeklist[date.getDay()];

            return `今天是: ${year}年${mon}月${day}号 ${hour}:${min}:${sec} 星期${week}`
        }
        function NowTime(){
           div.innerHTML = getMyDate(); 
        }
        NowTime();
        let n = setInterval(NowTime,1000);
    </script>
</body>
</html>
```

### ②方法二

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        div{
            display: inline-block;
            /* width: 300px; */
            height: 40px;
            border: 1px solid pink;
            text-align: center;
            line-height: 40px;
        }
    </style>
</head>
<body>
    <div></div>
    <script>
        const div = document.querySelector('div');
        function MyTime() {
            const date = new Date();
            let str = date.toLocaleString();//    yyyy/m/d hh:mm
            str = str.replace(/\//g, '-');//    将字符串中的/替换成-的方法
            div.innerHTML = str;
        }
        MyTime();
        setInterval(MyTime,1000);
        
    </script>
</body>
</html>
```

## 23.时间戳

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        div{
            display: inline-block;
            /* width: 300px; */
            height: 40px;
            border: 1px solid pink;
            text-align: center;
            line-height: 40px;
        }
    </style>
</head>
<body>
    <div></div>
    <script>
        const div = document.querySelector('div');
        function MyTime() {
            
            // // 01. getTime()
            // const date = new Date();
            // const time1 = date.getTime();
            // div.innerHTML = time1;

            //#########################################

            // // 02. +new Date()
            // let time2 = +new Date();
            // div.innerHTML = time2;

            //#########################################

            // // 03. Date.now()
            // let time3 = Date.now();
            // div.innerHTML = time3;

            //此方法只能得到当前的时间戳，
            //但前面两种可以返回指定时间的时间戳
            const date = new Date();
            const ToTime = +new Date('2022-4-1 18:30:00');
            const NowTime = date.getTime();
            div.innerHTML = ToTime - NowTime;
        }
        MyTime();
        setInterval(MyTime,1000);
        
    </script>
</body>
</html>
```

## 24.父节点，对比于(*dad.style.display = 'none';*)

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .dad{
            position: relative;
            background-color: greenyellow;
            width: 700px;
            height: 200px;
            margin: 100px auto;
            font-size: 50px;
            line-height: 200px;
            font-weight: 700;
            text-align:center;
        }
        .baby{
            position: absolute;
            right: 20px;
            top: 10px;
            width: 20px;
            height: 20px;
            background-color: skyblue;
            text-align: center;
            line-height: 20px;
            font-size: 16px;
            cursor:pointer;
        }
        .baby:hover{
            background-color: rgb(162, 213, 233);
        }
    </style>
</head>
<body>
    <div class="dad">
        我是广告
        <div class="baby">x</div>
    </div>
    <div class="dad" style="background-color: blueviolet;">
        我是广告
        <div class="baby">x</div>
    </div>
    <div class="dad"  style="background-color:blanchedalmond">
        我是广告
        <div class="baby">x</div>
    </div>
    <script>
        // const baby = document.querySelector('.baby');
        // // const dad = document.querySelector('.dad');

        // baby.addEventListener('click',function () {
        //     // dad.style.display = 'none';

        //     //baby.parentNode 返回 DOM 对象
        //     this.parentNode.style.display = 'none';
            
        // })
        const closeBtn = document.querySelectorAll('.baby');
        for (let i = 0; i < closeBtn.length; i++) {
            closeBtn[i].addEventListener('click',function(){
                this.parentNode.style.display = 'none';
            })
        }

        //###########   parentNode 父节点
        //###########   childNodes 子节点
    </script>
</body>
</html>
```

24.子节点

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        ul{
            background-color: #3cd1ff80;
            width: 200px;
        }
        li:nth-child(odd) {  
            /* odd：单号颜色，也可以是2n+1 */
            background-color: rgb(200, 255, 0);  
        } 
        li:nth-child(even) {  
            background-color: yellowgreen;  
        }
        li,p{
            cursor: pointer;
            list-style-type: none;
            width: 200px;
        }
        p:hover,li:hover{
            background-color: #c6c6c6;
        }
    </style>
</head>
<body>
    <ul>
        <li>第1个孩子</li>
        <li>第2个孩子</li>
        <li>第3个孩子</li>
        <li>第4个孩子</li>
        <li>第5个孩子</li>
        <p>我不需要变色</p>
    </ul>
    <script>
        const ul = document.querySelector('ul');
        ul.addEventListener('click',function(e){
            // alert(11);
            // console.log(e.target);
            // console.dir(e.target);
            // if(e.target.className === 'yuanshen')alert('wc O!');
            
            if(e.target.tagName === 'LI')
            e.target.style.color = 
                (e.target.style.color =='red') ? 'black' : 'red';
        })

        console.log(ul.children);//仅获取所有元素节点
        // console.log(ul.childNodes);//获得所有子节点
        const li2 = document.querySelector('ul li:nth-child(2)');
        console.log(li2.previousElementSibling);//上一个兄弟
        console.log(li2.nextElementSibling);//下一个兄弟

    </script>
</body>
</html>
```

## 25.子节点

```JavaScript
		console.log(ul.children);//仅获取所有元素节点
        // console.log(ul.childNodes);//获得所有子节点
        const li2 = document.querySelector('ul li:nth-child(2)');
        console.log(li2.previousElementSibling);//上一个兄弟
        console.log(li2.nextElementSibling);//下一个兄弟
```

## 26.增加节点

### 	①案例一

- *我是增加的节点*
- *我是原有的第一个节点*

```html
<style>
        div{
            width: 200px;
            height: 200px;
            background-color: pink;
        }
    </style>
</head>
<body>
    <ul>
        <li>我是原有的第一个节点</li>
    </ul>
    <script>
        // //1.创建节点
        // const div = document.createElement('div');
        // // console.log(div);
        // document.body.appendChild(div);

        //2.追加节点
        const ul = document.querySelector('ul');
        const li = document.createElement('li');
        li.innerHTML = '我是增加的节点';
        //插入到这个父元素的最后
        //父元素.appendChild(要插入的元素)
        // ul.appendChild(li);

        //插入到某个子元素的前面
        //父元素.insertBefore(要插入的元素,在哪个元素前面)
        ul.insertBefore(li,ul.children[0]);

    </script>
```

### 	②案例二

```html
<ul>
  <!-- 空的 -->
</ul>
```

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        li{
            list-style: none;
            /* width: 20px; */
            display: inline-block;
            /* height: 20px; */
            color: #333;
        }
        li:nth-child(2n+1) {  
            background-color: rgb(200, 255, 0);  
        } 
        li:nth-child(2n) {  
            background-color: yellowgreen;  
        }
    </style>
</head>
<body>
    <ul>
        <!-- 空的 -->
    </ul>
    <script>
        let data = [
            {
                title:'课程A',
                name:'张某',
                classname:'XX班级'
            },
            {
                title:'课程B',
                name:'李某',
                classname:'XX班级'
            },
            {
                title:'课程C',
                name:'王某',
                classname:'XX班级'
            },
            {
                title:'课程D',
                name:'赵某',
                classname:'XX班级'
            },
            {
                title:'课程E',
                name:'孙某',
                classname:'XX班级'
            },
            {
                title:'课程F',
                name:'周某',
                classname:'XX班级'
            }];
        const ul = document.querySelector('ul');
        for (let i = 0; i < data.length; i++) {
            const li = document.createElement('li');
            li.innerHTML = `
                    <p>${data[i].title}<p>
                    <p>${data[i].name}</p>
                    <p>${data[i].classname}</p>
            `
            ul.appendChild(li);
        }
    </script>
</body>
</html>
```

## 27.克隆节点

- 1
- 2
- 3
- 1

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <ul>
        <li>1</li>
        <li>2</li>
        <li>3</li>
    </ul>
    <script>
        //1 克隆节点
        const ul = document.querySelector('ul');
        
        // const li1 = document.querySelector('ul li:first-child');

        // const li1 = ul.children[0].cloneNode(true);
        // console.log(li1);
        // ul.appendChild(li1);

        ul.appendChild(ul.children[0].cloneNode(true));
    </script>
</body>
</html>
```

## 28.删除节点

- 2
- 3

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <ul>
        <li>1</li>
        <li>2</li>
        <li>3</li>
    </ul>
    <script>
        const ul = document.querySelector('ul');
        //删除节点
        //父元素.removeChild(子元素)
        ul.removeChild(ul.children[0]);
    </script>
</body>
</html>
```

## 29.m端事件(移动端)

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        div{
            width: 300px;
            height: 300px;
            background-color: pink;
        }
    </style>
</head>
<body>
    <div></div>
    <script>
        const div = document.querySelector('div');
        //1.触摸
        div.addEventListener('touchstart',function(){
            console.log('触碰');
        })
        //2.离开
        div.addEventListener('touchend',function(){
            console.log('离开了');
        })
        //3.移动
        div.addEventListener('touchmove',function(){
            console.log('移动');
        })
    </script>
</body>
</html>
```

## 30.移动端轮播图__插件的使用

<img src="/img_02/Snipaste_2024-09-22_22-13-47.png" alt="Snipaste_2024-09-22_22-13-47" style="zoom:33%;" />

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./css/swiper.min.css">
    <style>
        .box{
            width: 800px;
            height: 300px;
            background-color: pink;
            margin: 100px auto;
        }

        html, body {
      position: relative;
      height: 100%;
    }
    body {
      background: #eee;
      font-family: Helvetica Neue, Helvetica, Arial, sans-serif;
      font-size: 14px;
      color:#000;
      margin: 0;
      padding: 0;
    }
    .swiper-container {
      width: 100%;
      height: 100%;
    }
    .swiper-slide {
      text-align: center;
      font-size: 18px;
      background: #fff;

      /* Center slide text vertically */
      display: -webkit-box;
      display: -ms-flexbox;
      display: -webkit-flex;
      display: flex;
      -webkit-box-pack: center;
      -ms-flex-pack: center;
      -webkit-justify-content: center;
      justify-content: center;
      -webkit-box-align: center;
      -ms-flex-align: center;
      -webkit-align-items: center;
      align-items: center;
    }
    </style>
</head>
<body>
    <div class="box">
        <div class="swiper-container">
            <div class="swiper-wrapper">
              <div class="swiper-slide">Slide 1</div>
              <div class="swiper-slide">Slide 2</div>
              <div class="swiper-slide">Slide 3</div>
              <div class="swiper-slide">Slide 4</div>
              <div class="swiper-slide">Slide 5</div>
              <div class="swiper-slide">Slide 6</div>
              <div class="swiper-slide">Slide 7</div>
              <div class="swiper-slide">Slide 8</div>
              <div class="swiper-slide">Slide 9</div>
              <div class="swiper-slide">Slide 10</div>
            </div>
            <!-- Add Pagination -->
            <div class="swiper-pagination"></div>
          </div>
    </div>
    <script src="./js/swiper.min.js"></script>
    <script>
        var swiper = new Swiper('.swiper-container', {
          pagination: {
            el: '.swiper-pagination',
          },
        });
      </script>
</body>
</html>
```

## 31.定时器_延时函数

​		定时关闭

```html
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        img {
            width: 300px;
            height: 200px;
            position: fixed;
            left: 0;
            bottom: 0;
        }
    </style>
</head>

<body>
    <img src="./35.jpg" alt="">
    <script>
        //1.获取元素
        const img = document.querySelector('img');
        setTimeout(function () {
            img.style.display = 'none';
        }, 3000);
    </script>
</body>

</html>
```

## 32.location对象

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        a{
            /* text-decoration: none; */
            color: rgba(0, 0, 255, 0.647);
        }
        span{
            color: red;
        }
    </style>
</head>
<body>
    <div>
        <!-- <a href="http://www.baidu.com">
            支付成功！将在<span>5</span>秒之后自动跳转首页<br>
            (若网页未跳转，可点击文字直接跳转)
        </a> -->

        <!-- <form action="" method="get">
            <input type="text" name="username">
            <input type="password" name="password">
            <button>提交</button>
        </form>

        <a href="#/my">我的</a>
        <a href="#/friend">朋友</a>
        <a href="#/download">下载</a> -->

        <button class="reload">刷新</button>
    </div>
    <script>
        //########  2. location.href（获取完整的URL地址）  ##############
        // console.log(window.location);
        // console.log(location);
        // console.log(location.href);
        // location.href = 'http://www.baidu.com';

        // const a = document.querySelector('a');
        // let num = 5;
        // let timer = setInterval(function() {
        //     num--;
        //     a.innerHTML = `支付成功！将在<span>${num}</span>秒之后自动跳转首页<br>
        //     (若网页未跳转，可点击文字直接跳转)`;
        //     if (num === 0) {
        //         clearInterval(timer);
        //         location.href = 'http://www.baidu.com';
        //     }
        // },1000)

        //########  2. location.search（获取 ? 后面内容）###########
        // console.log(location.hash);

        //########  3. location.hash（获取 # 后面内容）  ############
        // console.log(location.hash);

        //############    4. reload 刷新页面    #################
        const reload = document.querySelector('.reload');
        reload.addEventListener('click',function(){
            location.reload();
            // location.reload(true);强制刷新 == Ctrl + F5
        })
    </script>
</body>
</html>
```

## 33.navigator对象

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        // console.log(navigator.userAgent);

        //检测 userAgent (浏览器信息)
        !(function () {
            const userAgent = navigator.userAgent;
            //验证是否为Android或iPhone
            const android = userAgent.match(/(Android);?[\s\/]+([\d.]+)?/);
            const iphone = userAgent.match(/(iPhone\sOS)\s([\d_]+)/);
            //如果是Android或iPhone，则跳转至移动站点
            if (android || iphone) {
                location.href = 'https://m.jd.com/';
            }
        })()
    </script>
</head>
<body>
    这是pc端的画面
</body>
</html>
```

## 34.history对象

<img src="/img_02/Snipaste_2024-09-22_22-18-34.png" alt="Snipaste_2024-09-22_22-18-34" style="zoom:33%;" />

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <button>后退</button>
    <button>前进</button>
    <script>
        const back = document.querySelector('button:first-child');
        const forward = back.nextElementSibling;
        back.addEventListener('click',function(){
            //后退一步
            // history.back();
            history.go(-1);
        })
        forward.addEventListener('click',function(){
            //前进一步
            // history.forward();
            history.go(1);
        })
    </script>
</body>
</html>
```

## 35.localStorage存储

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
        //***localStorage

        //***存储为字符串类型

        //***sessionStorage与localStorage基本相同;
        //***但生命周期为关闭浏览器窗口

        // //#####-----1.要存储一个名字 uname, pink老师-----###########
        // // localStorage.setItem(键，值)
        // localStorage.setItem('uname','pink老师');

        // //########---------2.获取方式----------###########
        // // localStorage.getItem(键)
        // console.log(localStorage.getItem('uname'));

        // //########---------3.删除本地存储----------###########
        // localStorage.removeItem('uname');
        // console.log(localStorage.getItem('uname'));
        
        // //########---------3.改----------###########
        // localStorage.setItem('uname','red老师');
        // console.log(localStorage.getItem('uname'));

        const obj = {
            uname: 'pink老师',
            age: 18,
            gender: '男'
        }
        // localStorage.setItem('obj',obj);                 X 错误的
        // console.log(localStorage.getItem('obj'));        X 错误的

        localStorage.setItem('obj',JSON.stringify(obj));
        console.log(JSON.parse(localStorage.getItem('obj')));
    </script>
</body>
</html>
```

## 36.1.BOM综合案例

<img src="/img_02/Snipaste_2024-09-22_22-20-50.png" alt="Snipaste_2024-09-22_22-20-50" style="zoom:33%;" />

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>学生就业统计表</title>
    <style>
        body{
            text-align: center;
            outline: 0;
        }
        form{
            margin: 30px;
        }
        input,select{
            width: 100px;
            height: 30px;
        }
        input{
            border: 1px solid rgb(79,79,79);
            padding: 0;  
            border-radius: 5%;
            box-sizing: border-box; /* 让元素的宽度和高度包括边框和内边距 */  
        }
        .add{
            width: 60px;
            height: 30px;
            background-color: #72bed0;
            border-radius: 5%;
            border: #8ebbc0 1px solid;
            color: white;
            cursor: pointer;
        }
        *:focus { 
            outline: 0;
       }
       .title {  
            display: flex;
            justify-content: flex-end;  /* 子项靠右对齐 */  
            align-items: center;  /* 子项垂直居中 */  
            margin: auto;  
            width: 900px;  
            height: 50px;  
            padding-right: 20px;
            background-color: #f2f2f2;  
            margin-bottom: 10px;
        }
        table{
            margin: auto;
            width: 920px;
            text-align: center;
        }
        table,th,td{
            border: 1px solid #ccc;
            border-collapse: collapse;
        }
        caption{
            font-style: 18px;
            margin-bottom: 10px;
            font-weight: 700;
        }
        tr{
            height: 40px;
            /* cursor: pointer; */
        }
        th{
            background-color: #f2f2f2;
        }

        .delete{
            display: block;
            width: 40px;
            height: 24px;
            margin: auto;
            background-color: #eb6b3c;
            color: #fff;
            border: #c4755b 1px solid;
            border-radius: 5%;
            cursor: pointer;
        }
        span{
            color: red;
        }
    </style>
</head>
<body>
    <h1>学生就业统计表</h1>
    <form class="info" autocomplete="off">
        <input type="text" class="uname" name="uname" placeholder="姓名">
        <input type="text" class="age" name="age" placeholder="年龄">
        <input type="text" class="salary" name="salary" placeholder="薪资">
        <select name="gender" class="gender">
            <option value="性别">性别</option>
            <option value="男">男</option>  
            <option value="女">女</option> 
        </select>
        <select name="city" class="city">
            <option value="城市">城市</option>  
            <option value="北京">北京</option>  
            <option value="上海">上海</option>  
            <option value="重庆">重庆</option>  
            <option value="成都">成都</option>
        </select>
        <button class="add">添加</button>
    </form>
    <div class="title">共有数据&nbsp;<span>0</span>&nbsp;条</div>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>姓名</th>
                <th>年龄</th>
                <th>性别</th>
                <th>薪资</th>
                <th>就业城市</th>
                <th>录入时间</th>
                <th>操作</th>
            </tr>
        </thead>
        <tbody>
            <!-- <tr>
                <td>test</td>
                <td>test</td>
                <td>test</td>
                <td>test</td>
                <td>test</td>
                <td>test</td>
                <td>test</td>
                <td><a class="delete">删除</a></td>
            </tr> -->
        </tbody>
    </table>
    <script>
        const initData = [
            {
                stuId: 1,
                uname: '小红',
                age: 22,
                gender: '女',
                salary: '12000',
                city: '北京',
                time: '2099/9/9 08:08:08'
            }
        ]
        // localStorage.setItem('data',JSON.stringify(initData));

        const arr = JSON.parse(localStorage.getItem('data')) || [];

        // console.log(arr);

        const tbody = document.querySelector('tbody');

        function render(){
            //利用map遍历数组，返回对应tr的数组
            
            const trArr = arr.map(function(ele,index){
                return `
                <tr>
                <td>${ele.stuId}</td>
                <td>${ele.uname}</td>
                <td>${ele.age}</td>
                <td>${ele.gender}</td>
                <td>${ele.salary}</td>
                <td>${ele.city}</td>
                <td>${ele.time}</td>
                <td><a class="delete" data-id="${index}">删除</a></td>
            </tr>
                `;
            });

            // console.log(trArr);

            tbody.innerHTML = trArr.join('');

            const span = document.querySelector('.title span');
            span.innerHTML = arr.length;
        }
        render();

        const info = document.querySelector('.info');
        const uname = document.querySelector('.uname');
        const age = document.querySelector('.age');
        const gender = document.querySelector('.gender');
        const salary = document.querySelector('.salary');
        const city = document.querySelector('.city');

        info.addEventListener('submit',function(e){
            e.preventDefault();
            if(!uname.value || !age.value || !salary.value || gender.value === '性别' || city.value === '城市'){
                return alert('输入内容不能为空');
            }


            if(!(/^\d+$/.test(age.value)) || !(/^\d+$/.test(salary.value))) {  
                return alert('‘年龄’或‘薪资’请输入纯数字');
            }

            if(/^\d+$/.test(uname.value)) {  
                return alert('‘名字’请输入纯汉字或英文字符');
            }

            // 正则表达式  /^\d+$/  的解释：
            // ^ 表示字符串的开始。
            // \d+ 表示一个或多个数字（\d是数字的意思，+表示一个或多个）。
            // $ 表示字符串的结束。

            arr.push({
                stuId: arr.length ? arr[arr.length - 1].stuId + 1 : 1,
                uname: uname.value,
                age: age.value,
                gender: gender.value,
                salary: salary.value,
                city: city.value,
                time: new Date().toLocaleString()
            });
            render();
            this.reset();   //重置表单

            localStorage.setItem('data',JSON.stringify(arr));
        });


        tbody.addEventListener('click',function(e){
            //判断是否点击的是删除按钮
            if(e.target.tagName === 'A'){
                // alert('test');
                // console.log(e.target.dataset.id);

                if(confirm('您确定要删除这条数据嘛？（删除后无法进行恢复！！）')){

                    if(confirm('再次确定您是否要删除？')){
                    arr.splice(e.target.dataset.id,1);

                    render();

                    localStorage.setItem('data',JSON.stringify(arr));
                    }

                }
            }
        })
    </script>
</body>
</html>
```

## 36.2.map方法和join方法

<img src="/img_02/Snipaste_2024-09-22_22-22-35.png" alt="Snipaste_2024-09-22_22-22-35" style="zoom:33%;" />

```javascript
const arr = ['red','blue','pink'];

        const newArr = arr.map(function(ele,index){
            // console.log(ele);//数组元素
            // console.log(index);//索引号
            return ele + '颜色';
        })
        console.log(newArr);

        console.log(newArr.join());
        console.log(newArr.join(''));
        console.log(newArr.join('|'));
```

## 37.正则表达式的使用

<img src="/img_02/Snipaste_2024-09-22_22-23-29.png" alt="Snipaste_2024-09-22_22-23-29" style="zoom:33%;" />

```javascript
const str = '我们在学习前端，希望学习前端能高薪毕业';
        const reg = /前端/;
        // reg.test(str);

        //是否含有这个词
        // console.log(reg.test(str));  //返回 true 或 flase

        console.log(reg.exec(str)); //返回数组,否则为null
```

## 38.1.元字符

```javascript
//####-----壹-----#####

        //  ^       为开头
        //  $       为结尾
        //  \d      为 0~9
        //  \D      等于[^0-9]
        //  \w      等于[A-Za-z0-9_]
        //  \W      等于[^A-Za-z0-9_]
        //  \s      等于[\t\r\n\v\f]
        //  \S      等于[^\t\r\n\v\f]
        //  *       >= 0次
        //  +       >= 1次
        //  ?       0或1次
        //  {n}     重复n次
        //  {n,}    重复n次或更多次
        //  {n,m}   重复n到m次
        //  [abc]   包含a/b/c都返回true（三选一）
        //  [a-z]   a~z,只选一个
        //  [^a-z]  除了a~z

        // console.log(/哈/.test('哈'));    //true
        // console.log(/哈/.test('哈哈'));  //true
        // console.log(/哈/.test('二哈'));  //true
        // console.log(/哈/.test('哈巴狗'));  //true

        // console.log(/^哈/.test('哈巴狗'));  //true
        // console.log(/^哈/.test('二哈'));  //false

        // console.log(/^哈$/.test('哈'));  //true
        // console.log(/^哈$/.test('哈哈'));  //false
        
        // console.log(/哈$/.test('二哈'));  //true
        // console.log(/哈$/.test('哈巴狗'));  //false
        
        // console.log(/^哈*$/.test(''));  //true
        // console.log(/^哈*$/.test('哈'));  //true
        // console.log(/^哈*$/.test('哈哈'));  //true
        // console.log(/^哈*$/.test('哈哈哈'));  //true

        console.log(/^[abc]$/.test('a'));  //true
        console.log(/^[abc]$/.test('ab'));  //false
        console.log(/^[abc]{2}$/.test('ab'));  //true

        //####-----贰-----#####

        //  i 正则匹配时不区分大小写（ignore）
        console.log(/a/i.test('a'));  //true
        console.log(/a/i.test('A'));  //true

        //  g 匹配所有满足正则表达式的结果（global）

        //####-----叁-----#####
        // 字符串.replace(/正则表达式/,'替换的文字');
        const str = '我喜欢你！我喜欢你！！';
        const re0 = str.replace(/喜欢/,'**');//我**你！我喜欢你！！
        const re1 = str.replace(/喜欢/g,'**');//我**你！我**你！！

        console.log(re0);
        console.log(re1);
```

## 38.2.test

```javascript
let input = document.querySelector('input');
        let span = input.nextElementSibling;
        input.addEventListener('blur',function(){
            if(/^[a-zA-Z0-9-_]{6,16}$/.test(input.value)){
                span.className = 'right';
                span.innerHTML = '输入正确';
            }else{
                span.className = 'error';
                span.innerHTML = '只能输入6~16位英文数字下划线';
            }
        })
```

## 38.3.test

<img src="/img_02/Snipaste_2024-09-22_22-28-10.png" alt="Snipaste_2024-09-22_22-28-10" style="zoom:33%;" />

```javascript
const tx = document.querySelector('textarea');
        const btn = document.querySelector('button');
        const div = document.querySelector('div');
        btn.addEventListener('click',function(){
            // console.log(tx.value);
            div.innerHTML = tx.value.replace(/第五人格/g,'****');
            tx.value = '';
        })
```

## 39.闭包

<img src="/img_02/Snipaste_2024-09-22_22-30-19.png" alt="Snipaste_2024-09-22_22-30-19" style="zoom:33%;" />

```javascript
//在控制台调用fun()时，令i = 100时修改无效
        function count(){
            let i = 0;
            function fn(){
                i++;
                console.log(`函数被调用了${i}次`);
            }
            return fn;
        }
        const fun = count();
```

## 40.函数动态参数

```javascript
function getSum(){
            //arguments 动态参数 只存在于 函数里面
            //是伪数组

        // console.log(arguments);

        let sum = 0;
        for (let i = 0; i < arguments.length; i++) {
            sum += arguments[i];
        }
        console.log(sum);//		9
    }
    getSum(2,3,4);
```

## 41.函数参数剩余参数

```javascript
function getSum(a,b,...arr){
            console.log(arr);
        }
        getSum(1,2);//  0
        getSum(1,2,3);//    3

        //展开预算符
        const arr1 = [1,2,3];
        console.log(...arr1);// 1 2 3

        console.log(Math.max(1,2,3));// 3
        console.log(Math.max(...arr1));// 3

        const arr2 = [4,5,6];
        const arr = [...arr1,...arr2];
        console.log(arr);// 1 2 3 4 5 6
```

## 42.箭头函数

```javascript
// const fn = function fn(){
        //     console.log(123);
        // }

        // const fn = () => {
        //     console.log(123);
        // }
        // fn();

        // const fn = (x) => {
        //     console.log(x);
        // }
        // fn(1);

        //只有一个形参的时候，才能省略小括号
        //只有一行代码的时候，可以省略大括号

            // const fn = x => x + x;
            // console.log(fn(1));
            //为 2（不用加return）

        // const form = document.querySelector('form');
        // form.addEventListener('click',ev => ev.preventDefault());

        //可以直接返回一个对象
        const fn = uname => ({name: uname});
        // fn('刘德华');
        console.log(fn('刘德华'));
```

## 43.箭头函数里面的this

```javascript
//指向obj
        const obj = {
            name: 'andy',
            sayHi: function () {
                console.log(this);
            }
        }
        console.log(obj.sayHi());

        //指向window （直接指向它父亲的this）
        const obj2 = {
            name: 'pink老师',
            sayHi:  () => {
                console.log(this);
            }
        }
        console.log(obj2.sayHi());
```

## 44.数据解构

```javascript
		// const arr = [100,60,80];
        // const [max,min,avg] = arr;
        // console.log(max);

        let a = 1;
        let b = 2;
        [b,a] = [a,b];
        console.log(a,b);// 2 1
```

## 45.对象解构

```javascript
/ const obj = {
        //     uname: 'pink老师',
        //     age:18
        // }

        // const {uname,age} = {uname: 'pink老师',age:18};
        // console.log(uname);
        // console.log(age);

        // const {uname:username,age} = {uname: 'pink老师',age:18};
        // console.log(username);
        // console.log(age);


        
        //解构数组对象
        const pig = [
            {
                uname: '佩奇',
                age: 6
            }
        ]
        const [{uname,age}] = pig;
        console.log(uname);
        console.log(age);
    </script>
```

## 46.foreach语法

```javascript
const arr = ['red','green','pink'];
        arr.forEach(function(item,index){
            console.log(item);
            console.log(index);
            //red
            //0
            //green
            //1
            //pink
            //2
        })
```

## 47.构造函数

```javascript
function Pig(name,age,gender){
            this.name = name;
            this.age = age;
            this.gender = gender;
        }
        const Peppa = new Pig('佩奇',6,'女');
        console.log(Peppa);
```

## 48.Object静态方法

```javascript
const o = {uname:'pink',age:18}
        // for (const k in o) {
        //     console.log(k);
        //     console.log(o[k]);
        // }

        console.log(Object.keys(o));//uname age
        console.log(Object.values(o));//pink 18

        //对象的拷贝
        const obj = {};
        Object.assign(obj,o);
        console.log(obj);//age: 18,uname: "pink"

        Object.assign(o,{gender:'女'});
        console.log(o);//age: 18,uname: "pink",gender:'女'
```

## 49.数组reduce方法

```javascript
const arr = [1,5,8];

        // //无初始值
        // const total = arr.reduce(function(prev,current){
        //     return prev + current;
        // })
        // console.log(total);

        //有初始值
        const total = arr.reduce((prev,current) => prev + current,10);
        console.log(total);//  24
```

## 50.Array常用方法

![c11e2618-cdab-40d2-a882-4f6089da5cbf](/img_02/c11e2618-cdab-40d2-a882-4f6089da5cbf.png)

```javascript
//1.find
        
        // const arr = ['red','blue','green'];
        // const re = arr.find(item => item === 'blue');
        // console.log(re); // blue
        
        // const arr = [
        //     {
        //         name:'小米',
        //         price: 1999
        //     },
        //     {
        //         name:'华为',
        //         price: 3999
        //     }
        // ]
        // const re = arr.find(item => item.name === '小米');
        // console.log(re);//name: "小米",price: 1999

        //2.every
        // const arr1 = [10,20,30];
        // const flag = arr1.every(item => item >= 10);
        // const flag1 = arr1.some(item => item >= 20);
        // console.log(flag);// true
        // console.log(flag1);// true
```

<img src="/img_02/f4410db8-945a-48f2-bd1d-cf7eb4af6b0c.png" alt="f4410db8-945a-48f2-bd1d-cf7eb4af6b0c" style="zoom:50%;" />

## 51.伪数组转换为真数组

<img src="/img_02/Snipaste_2024-09-22_22-54-03.png" alt="Snipaste_2024-09-22_22-54-03" style="zoom:33%;" />

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <ul>
        <li>1</li>
        <li>2</li>
        <li>3</li>
    </ul>
    <script>
        //Array.from(lis);
        const lis = document.querySelectorAll('ul li');
        console.log(lis);
        const liss = Array.from(lis);
        liss.pop();//移除liss数组的最后一个元素
        console.log(liss);
    </script>
</body>
</html>
```

## 52.构造函数

```javascript
function Star(uname,age){
            this.uname = uname;
            this.age = age;
        }
        Star.prototype.sing = () => console.log('唱歌');

        const ldh = new Star('刘德华',55);
        const zxy = new Star('张学友',58);
        
        console.log(ldh.sing === zxy.sing);// true
```

## 53.数组扩展方法

```javascript
const arr = [1,2,3];
        Array.prototype.max = function() {
            return Math.max(...this);
        } 
        console.log(arr.max());// 3
        const arr2 = [1,1,1];
        console.log(arr2.max());// 1
```

## 54.constructor属性

<img src="/img_02/Snipaste_2024-09-23_08-28-11.png" alt="Snipaste_2024-09-23_08-28-11" style="zoom:33%;" />

```javascript
// function Star() {
        // }
        // const ldh = new Star();
        // console.log(Star.prototype.constructor === Star);


        function Star() {
        }
        // Star.prototype.sing = () => console.log('唱歌');
        // Star.prototype.dance = () => console.log('跳舞');

        Star.prototype = {
            constructor: Star,
            sing: () => console.log('唱歌'),
            dance: () => console.log('跳舞'),
        }
        console.log(Star.prototype);
```

## 55.对象原型

<img src="/img_02/fbbed9e2-6785-4202-b2e2-2c7220d904cf.png" alt="fbbed9e2-6785-4202-b2e2-2c7220d904cf" style="zoom:33%;" />

```javascript
function Star(){    }
        const ldh = new Star();
        // console.log(ldh.__proto__);
        //对象原型__proto__指向该构造函数的原型对象
        console.log(ldh.__proto__  === Star.prototype);//true

        console.log(ldh.__proto__.constructor  === Star);//true
```

## 56.原型继承

<img src="/img_02/Snipaste_2024-09-23_08-36-00.png" alt="Snipaste_2024-09-23_08-36-00" style="zoom:50%;" />

```javascript
function Person(){
            this.eyes = 2;
            this.head = 1;
        }

        function Woman(){
            
        }
        Woman.prototype = new Person();
        Woman.prototype.constructor = Woman;

        Woman.prototype.baby = () => console.log('宝贝');
        
        const red = new Woman();
        console.log(red);

        function Man(){

        }
        Man.prototype = new Person();
        Man.prototype.constructor = Man;

        const pink = new Man();
        console.log(pink);
```

<img src="/img_02/8ed4172c-d47f-4c26-bbdc-27110758c541.png" alt="8ed4172c-d47f-4c26-bbdc-27110758c541" style="zoom:50%;" />

<img src="/img_02/v2-8c24510662b036a820c9cc58193ccedf_1440w.jpg" alt="img" style="zoom:60%;" />

<img src="/img_02/7ebc96b5-25cc-4f5f-91a4-fc3e1fab0886.png" alt="7ebc96b5-25cc-4f5f-91a4-fc3e1fab0886" style="zoom:50%;" />

## 57.综合案例

<img src="/img_02/Snipaste_2024-09-23_08-46-08.png" alt="Snipaste_2024-09-23_08-46-08" style="zoom:50%;" />

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* .modal{
            width: 300px;
            height: 120px;
            border: 1px solid black;
        }

        .modal i{
            position: absolute;
            right: 0;
        } */
        .modal {
            /* display: none; */
            width: 300px;
            height: 80px;
            position: fixed;
            left: 50%;
            top: 50%;
            transform: translate(-50%, -50%);
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            z-index: 1000; /* 确保模态框在其他内容之上 */
        }

        .modal .header {
            font-weight: bold;
            margin-bottom: 10px;
        }

        .modal .header i {
            float: right;
            cursor: pointer;
        }

        .modal .body {
            margin-bottom: 0; /* 移除默认的下边距 */
        }

        /* 关闭按钮的样式 */
        .modal .header i:hover {
            color: red;
        }

        .body{
            text-align: center;
        }
    </style>
</head>
<body>
    <button id="delete">删除</button>
    <button id="login">登录</button>

    <!-- <div class="modal">
        <div class="header">温馨提示 <i>X</i></div>
        <div class="body">您没有删除权限操作</div>
    </div> -->

    <script>
        //1. Modal 构造函数封装 - 模态框
        function Modal(title = '', message = '') {
          //创建 modal 模态框盒子
          //1.1 创造div标签
          this.modalBox = document.createElement('div');
          //1.2 给div标签添加类名为 modal
          this.modalBox.className = 'modal';
          //1.3 modal盒子内部填充 2个div标签并且修改文字内容
          this.modalBox.innerHTML = `
            <div class="header">${title} <i>X</i></div>
            <div class="body">${message}</div>
          `;
        //   console.log(this.modalBox);
        }

        // new Modal('温馨提示','您没有删除权限操作');
        // new Modal('友情提示','您还没有注册呢');

        //2.给构造函数原型对象挂载 open 方法
        Modal.prototype.open = function(){
            const box = document.querySelector('.modal');
            box && box.remove();
            
            document.body.append(this.modalBox);

            this.modalBox.querySelector('i').addEventListener('click',() =>{
                this.close();
            })
        }

        Modal.prototype.close = function(){
            this.modalBox.remove();
        }

        document.querySelector('#delete').addEventListener('click',() =>{
            const del = new Modal('温馨提示','您没有删除权限操作');
            del.open();
        })

        document.querySelector('#login').addEventListener('click',() =>{
            const del = new Modal('友情提示','您还没有注册呢');
            del.open();
        })
    </script>
</body>
</html>
```

## 58.浅拷贝

<img src="/img_02/Snipaste_2024-09-23_08-52-14.png" alt="Snipaste_2024-09-23_08-52-14" style="zoom:50%;" />



```javascript
        const obj = {
            uname: 'pink',
            age: 18,
            family:{
                baby: '小pink',//只拷贝最外层
            }
        }

        //浅拷贝（1）只拷贝最外层
        const o = {...obj};
        console.log(o);
        o.age = 20;
        console.log(o);
        console.log(obj);

        //（2）
        const o = {};
        Object.assign(o,obj);
        console.log(o);
        o.age = 20;
        o.family.baby = '老pink';//只拷贝最外层
        console.log(o);
        console.log(obj);
```

<img src="/img_02/Snipaste_2024-09-23_08-54-06.png" alt="Snipaste_2024-09-23_08-54-06" style="zoom:50%;" />

## 59.递归函数

```javascript
// let i = 1;
        // function fn(){
        //     console.log(`这是第${i}次`);
        //     if(i >= 6){
        //         return;
        //     }
        //     i++;
        //     fn();
        // }
        // fn();

        function getTime(){
            document.querySelector('div').innerHTML = new Date().toLocaleString();
            setTimeout(getTime,1000);
            getTime();
        }
        getTime();
```

## 60.1.深拷贝

```javascript
const obj = {
            uname: 'pink',
            age: 18,
            hobby: ['乒乓球', '足球'],
            family: {
                baby: '小pink',
            }
        }
        const o = {};

        function deepCopy(newObj, oldObj) {
            for (let key in oldObj) {
                if (oldObj[key] instanceof Array) {
                    newObj[key] = [];
                    deepCopy(newObj[key], oldObj[key]);
                } else if (oldObj[key] instanceof Object) {
                    newObj[key] = {};
                    deepCopy(newObj[key], oldObj[key]);
                } else {
                    newObj[key] = oldObj[key];
                }

            }
        }
        deepCopy(o, obj);

        o.age = 20;
        o.hobby[0] = '篮球';
        o.family.baby = '老pink';
        console.log(o);
        console.log(obj);
```

## 60.2.深拷贝_lodash

```javascript
//<script src="./lodash.min.js"></script>
const obj = {
            uname: 'pink',
            age: 18,
            hobby: ['乒乓球', '足球'],
            family: {
                baby: '小pink',
            }
        }

        const o = _.cloneDeep(obj);

        o.age = 20;
        o.hobby[0] = '篮球';
        o.family.baby = '老pink';
        console.log(o);
        console.log(obj);
```

## 60.3.深拷贝_JSON

```javascript
const obj = {
            uname: 'pink',
            age: 18,
            hobby: ['乒乓球', '足球'],
            family: {
                baby: '小pink',
            }
        }

        const str = JSON.stringify(obj);
        const o = JSON.parse(str);

        o.age = 20;
        o.hobby[0] = '篮球';
        o.family.baby = '老pink';
        console.log(o);
        console.log(obj);
```

## 61.throw抛出异常

```javascript
function fn(x,y){
            if(!x || !y){
                throw new Error('没有参数传递进来');
            }
            return x + y;
        }
        console.log(fn());//没有参数传递进来
```

## 62.try-catch捕获异常<img src="/img_02/Snipaste_2024-09-23_09-20-33.png" alt="Snipaste_2024-09-23_09-20-33" style="zoom:50%;" />

```javascript
//<p>123</p>
   // <script>
        function fn(){
            try{
                const p = document.querySelector('.p');//错误
                p.style.color = 'red';
            }catch(err){
                //不中断程序的执行

                debugger;//调试
                
                console.log(err.message);
                throw new Error('你看看，选择器错误了吧');
                //需要加return 中断程序
                // return;
            }
            finally{
                //不管你程序对不对，一定会执行的代码
                alert('弹出对话框');
            }
            console.log('如果有错误，我不会被执行');
        }
        fn();
   // </script>
```

## 63.1.改变this指向_call

```javascript
 const obj = {
        uname: 'pink',
    }
function fn(x,y){
    console.log(this);//原本指向window
    console.log(x + y);
}
fn.call(obj,1,2);//指向obj
```

## 63.2.改变this指向_apply

```javascript
const obj = {
            age: 18,
        }
function fn(x, y) {
    console.log(this);//原指window
    console.log(x + y);
}
fn.apply(obj, [1, 2]);//{age: 18}，3

// const max = Math.max(1, 2, 3);
// console.log(max);

const arr = [114, 514, 191];
const max = Math.max.apply(Math, arr);
// const max = Math.max.apply(null, arr);
console.log(max);// 514
console.log(Math.max(...arr));// 514
```

## 63.3.改变this指向_bind

```javascript
const obj = {
            age: 18,
        }
function fn() {
    console.log(this);//原 window
}
//bind不会调用函数
const fun = fn.bind(obj);
// console.log(fun);
fun();
```

## 64.性能优化之防抖

<img src="/img_02/df0c606b-b05c-457c-a38a-b4a664c93be7.png" alt="df0c606b-b05c-457c-a38a-b4a664c93be7" style="zoom:25%;" />

```html
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .box {
            width: 500px;
            height: 500px;
            background-color: #ccc;
            color: #fff;
            text-align: center;
            font-size: 100px;
        }
    </style>
</head>

<body>
    <div class="box"></div>
    <script src="./lodash.min.js"></script>
    <script>

        // _.debounce(func,[wait=0],[options=])

        const box = document.querySelector('.box');
        let i = 1;
        function mouseMove() {
            box.innerHTML = i++;
        }
        // box.addEventListener('mousemove',mouseMove);

        // box.addEventListener('mousemove', _.debounce(mouseMove, 500));

        function debounce(fn, t) {
            let timer;
            return function () {
                if (timer) clearTimeout(timer);
                timer = setTimeout(function () {
                    fn();
                }, t);
            }
        }
        box.addEventListener('mousemove', debounce(mouseMove, 500));
    </script>
</body>

</html>
```

## 65.性能优化之节流

```html
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .box {
            width: 500px;
            height: 500px;
            background-color: #ccc;
            color: #fff;
            text-align: center;
            font-size: 100px;
        }
    </style>
</head>

<body>
    <div class="box"></div>
    <script src="./lodash.min.js"></script>
    <script>

        // _.throttle(func,时间)

        const box = document.querySelector('.box');
        let i = 1;
        function mouseMove() {
            box.innerHTML = i++;
        }
        // box.addEventListener('mousemove',mouseMove);

        // box.addEventListener('mousemove', _.throttle(mouseMove, 1000));

        function throttle(fn, t) {
            let timer = null;
            return function () {
                if (!timer) {
                    timer = setTimeout(function () {
                        fn();
                        timer = null;
                    }, t);

                }
            }
        }
        box.addEventListener('mousemove', throttle(mouseMove, 1000));
    </script>
</body>

</html>
```

## 66.节流综合案例

```html
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        .container {
            width: 1200px;
            margin: 0 auto;
        }

        .video video {
            width: 100%;
            padding: 20px 0;
        }

        .elevator {
            position: fixed;
            top: 280px;
            right: 20px;
            z-index: 999;
            background: #fff;
            border: 1px solid #e4e4e4;
            width: 60px;
        }

        .elevator a {
            display: block;
            padding: 10px;
            text-decoration: none;
            text-align: center;
            color: #999;
        }

        .elevator a.active {
            color: #1286ff;
        }

        .outline {
            padding-bottom: 300px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <a href="http://pip.itcast.cn">
                <!-- <img src="https://pip.itcast.cn/img/logo_v3.29b9ba72.png" alt=""/> -->
                <img src="./logo_v3.29b9ba72.png" alt="" />
            </a>
        </div>
        <div class="video">
            <!-- <video src="https://v.itheima.net/LapADhV6.mp4" controls></video> -->
            <video src="./LapADhV6.mp4" controls muted></video>
        </div>
        <div class="elevator">
            <a href="javascript:;" data-ref="video">视频介绍</a>
            <a href="javascript:;" data-ref="intro">课程简介</a>
            <a href="javascript:;" data-ref="outline">评论列表</a>
        </div>
    </div>
    <!-- <script src="https://cdn.jsdelivr.net/npm/lodash@4.17.21/lodash.min.js"></script> -->
    <script src="./lodash.min.js"></script>
    <script>
        const video = document.querySelector('video');
        video.ontimeupdate = _.throttle(() => {
            // console.log(video.currentTime);//获得当前的视频时间
            localStorage.setItem('currentTime', video.currentTime);
        }, 1000);
        video.onloadeddata = () => {
            video.currentTime = localStorage.getItem('currentTime') || 0;
        }
    </script>
</body>

</html>
```

